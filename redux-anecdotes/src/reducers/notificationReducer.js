const notificationReducer = (state = 'initial notification', action) => {
    console.log(action)
    switch (action.type) {
      default:
        return state
    }
  }

export default notificationReducer